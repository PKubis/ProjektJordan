@page
@model LoginWith2faModel
@{
    ViewData["Title"] = "Dwuskładnikowa weryfikacja";
}

<h1 class="text-orange">@ViewData["Title"]</h1>
<hr />
<p>Twój login jest chroniony aplikacją uwierzytelniającą. Wpisz poniżej swój kod uwierzytelniający.</p>
<div class="row">
    <div class="col-md-4">
        <form method="post" asp-route-returnUrl="@Model.ReturnUrl">
            <input asp-for="RememberMe" type="hidden" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-floating">
                <input asp-for="Input.TwoFactorCode" class="form-control" autocomplete="off" />
                <label asp-for="Input.TwoFactorCode" class="text-dark">Podaj kod</label>
                <span asp-validation-for="Input.TwoFactorCode" class="text-danger"></span>
            </div>
            <div>
                <div class="checkbox">
                    <label asp-for="Input.RememberMachine" class="form-label">
                        <input asp-for="Input.RememberMachine" />
                        @Html.DisplayNameFor(m => m.Input.RememberMachine)
                    </label>
                </div>
            </div>
            <div>
                <button type="submit" class="w-100 btn btn-lg btn-warning">Zaloguj</button>
            </div>
        </form>
    </div>
</div>
<p>
    Nie masz dostępu do swojego urządzenia uwierzytelniającego? Możesz
    <a id="recovery-code-login" asp-page="./LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl">zalogować się za pomocą kodu odzyskiwania</a>.
</p>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}