@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Rejestracja";
}

<h1 class="text-orange">@ViewData["Title"]</h1>
<h2 id="external-login-title">Powiąż swoje @Model.ProviderDisplayName konto.</h2>
<hr />

<p id="external-login-description" class="text-info">
    Pomyślnie dokonałeś uwierzytelnienia za pomocą <strong>@Model.ProviderDisplayName</strong>.
    Wpisz poniżej adres e-mail tej witryny i kliknij przycisk Zarejestruj, aby zakończyć
    Logowanie.
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-floating">
                <input asp-for="Input.Email" class="form-control" autocomplete="email" />
                <label asp-for="Input.Email" class="text-dark">Email</label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-warning">Rejestruj</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
